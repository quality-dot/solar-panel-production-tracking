# Encryption Service Configuration
# Copy this file to .env.encryption and set your values
# IMPORTANT: Keep this file secure and never commit it to version control

# Master encryption key (32 bytes, base64 encoded)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_MASTER_KEY=your_32_byte_base64_encoded_master_key_here

# Key rotation settings (optional, defaults to 90 days)
ENCRYPTION_KEY_ROTATION_DAYS=90

# Encryption algorithm (optional, defaults to aes-256-gcm)
ENCRYPTION_ALGORITHM=aes-256-gcm

# Key storage path (optional, defaults to backend/keys)
ENCRYPTION_KEYS_DIR=backend/keys

# Performance settings (optional)
ENCRYPTION_PERFORMANCE_MODE=production
ENCRYPTION_HARDWARE_ACCELERATION=true

# Security settings (optional)
ENCRYPTION_AUDIT_LOGGING=true
ENCRYPTION_COMPLIANCE_MODE=strict

# Example of generating a secure master key:
# 1. Open terminal/command prompt
# 2. Run: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# 3. Copy the output and paste it as ENCRYPTION_MASTER_KEY value
# 4. Keep this key secure and backup safely
